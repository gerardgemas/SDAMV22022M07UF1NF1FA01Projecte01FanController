<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="estilSwitches" parent="Theme.AppCompat.Light">
        <!-- Colors del thumb, per l'estat d'activat i desactivat -->
        <item name="android:thumbTint">@color/colors_switch_thumb</item>
        <!-- Colors de la ranura, per l'estat d'activat i desactivat -->
        <item name="android:trackTint">@color/colors_switch_ranura</item>
    </style>
    <style name="estilSeekBars" parent="Theme.AppCompat.Light">
        <!-- Configurem, respectivament, la visualització del thumb, dels ticks i del progress -->
        <item name="android:thumb">@drawable/seekbar_thumb</item>
        <item name="android:tickMark">@drawable/seekbar_tick</item>
        <item name="android:progressDrawable">@drawable/seekbar_progress</item>
    </style>
</resources>